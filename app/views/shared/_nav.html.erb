
<nav class="bg-orange-200 py-4 w-full shadow-sm">
  <div class="container mx-auto">
    <div class="flex justify-between items-center">
      <!-- Logo -->
      <a class=" text-2xl font-bold" href="/">
        Offers
      </a>

      <!-- Navigation Links -->
      <div class="space-x-4 mr-3">
        <% if current_user %>
          <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white" %>
        <% else %>
          <%= link_to "Sign In", user_session_path, data: { turbo_method: :post } %>
          <%= link_to "Sign Up", new_user_registration_path, data: { turbo_method: :get } %>
        <% end %>
      </div>
    </div>
  </div>
</nav>